# Deprecated: Admin ID Gating System

> [!WARNING]
> The Admin ID Gating system has been completely decommissioned. Accounts can now be registered and logged into (including Google and GitHub OAuth) directly without requiring any Admin ID code or verification step.

For historical reference, the management tool is located at `/opt/aurelius/infra/manage-codes.sh`, but it is no longer required for user access.